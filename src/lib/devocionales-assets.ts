/**
 * Archivos bajo `public/`. Imagen por defecto del tema activo cuando no hay
 * `imagen_url` guardada (ya no se configura por formulario).
 */
export const DEVOCIONAL_IMAGEN_TEMA_PRINCIPAL = "/tema1.png";
